apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sitecore.fullname" . }}-test-connection"
  labels:
    {{- include "sitecore.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sitecore.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
